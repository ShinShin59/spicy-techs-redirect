/**
 * Shared border hover effect for panels (Developments tile and others).
 * Use on the panel container so the border lightens on hover even when only inner slots are clickable.
 */
export const PANEL_BORDER_HOVER_CLASS =
  "border border-zinc-700 hover:border-zinc-500 transition-colors"
